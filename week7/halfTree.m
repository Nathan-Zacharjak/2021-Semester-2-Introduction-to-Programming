function halfTree(n)
    % Takes a number n and prints to the console a half christmas tree
    % pattern with n repetitions.
    % n must be greater than one
    for segment = 1:n
        treeSegment(segment);
    end
end